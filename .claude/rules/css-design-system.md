---
description: CSS design tokens, typography choices, and reusable component classes — use these before introducing new styles
globs: ["*.html", "*.css"]
alwaysApply: false
---

## Typography

- Body: `'Outfit'` (Google Fonts)
- Code: `'JetBrains Mono'`

## CSS Custom Properties

```css
--primary: #2563EB    --secondary: #10B981   --accent: #F59E0B
--danger: #EF4444     --purple: #8B5CF6      --bg: #F0F4FF
--surface: #FFFFFF    --text: #0F172A        --text2: #475569
--border: #E2E8F0     --nav-h: 64px          --radius: 20px
--radius-sm: 12px     --radius-xs: 8px       --navy-text: #1E40AF
```

## Reusable CSS Classes

`.card` variants | `.btn` variants | `.tag` colour variants | `.progress-track`/`.progress-fill` | `.concept-box` | `.code-block` | `.lab-container`/`.lab-input`/`.lab-output` | `.section-card` | `.screen-header` | `.empty-state`
